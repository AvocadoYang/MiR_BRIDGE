[project]
name = "mir-bridge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cowsay>=6.1",
    "loguru>=0.7.3",
    "pydantic>=2.13.4",
    "pyyaml>=6.0.3",
]
